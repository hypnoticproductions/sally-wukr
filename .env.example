# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Gemini API Key (for voice streaming - Live API)
# Get your API key from: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Note: The GEMINI_API_KEY for the Edge Function is configured automatically
# in the Supabase dashboard under Edge Functions secrets

# Stripe Configuration (for payment processing)
VITE_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here
# Note: STRIPE_SECRET_KEY for Edge Functions is configured automatically
# in the Supabase dashboard under Edge Functions secrets
# Note: STRIPE_PRICE_ID for Edge Functions is configured automatically
# Product: 30-Day Strategic Profile Retention - $30
# Use the setup-stripe-products edge function to create products in your Stripe account

# Manus Integration (optional - for workflow automation)
# Get your API key from: https://open.manus.ai/docs
# Note: MANUS_API_KEY for Edge Functions is configured automatically
# in the Supabase dashboard under Edge Functions secrets
# Note: MANUS_WEBHOOK_SECRET for Edge Functions is set to 'contancybearsfruit'
# This secret is used by Manus to authenticate webhook calls to sally-webhook endpoint

# Telnyx Configuration (for voice calling)
# Get your API key from: https://portal.telnyx.com/#/app/api-keys
# Note: TELNYX_API_KEY for Edge Functions is configured automatically
# in the Supabase dashboard under Edge Functions secrets
# Note: TELNYX_PHONE_NUMBER should be your purchased Telnyx phone number (e.g., +15551234567)
# Note: TELNYX_CONNECTION_ID is the Application ID from your Call Control Application
#       Found in Telnyx Portal under Voice > Call Control (or Voice > Applications)
#       This is NOT the same as a SIP Connection ID from Voice > Connections
#       Format: conn_xxxxx or a numeric ID
# Note: RICHARD_PHONE_NUMBER is Richard's phone number for call transfers (optional)
